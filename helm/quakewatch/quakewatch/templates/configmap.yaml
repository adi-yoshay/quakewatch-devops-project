apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  labels:
    app: quakewatch
data:
  APP_MESSAGE: "{{ .Values.config.APP_MESSAGE }}"
  APP_MODE: "{{ .Values.config.APP_MODE }}"

